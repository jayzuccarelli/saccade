# Preferences (semantic memory)

Plain-language notes about when to help and when to stay quiet. Focus reads this
before deciding to interrupt. Hand-edited for now; later the system learns and
updates it from what you accept and ignore.

- Don't interrupt during deep focus unless it's urgent.
- A short, friendly heads-up is better than a long explanation.
